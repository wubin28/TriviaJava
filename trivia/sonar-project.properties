# required metadata
sonar.projectKey=trivia:java
sonar.projectName=trivia.java
sonar.projectVersion=1.0

# optional description
sonar.projectDescription=Taming the bad code of Java version of Trivia

# path to source directories (required)
sonar.sources=C:/OOR/Archetypes/github/TriviaJava/trivia/src/main/java

# path to test source directories (optional)
sonar.tests=C:/OOR/Archetypes/github/TriviaJava/trivia/src/test/java

# path to project binaries (optional), for example directory of Java bytecode
sonar.binaries=C:/OOR/Archetypes/github/TriviaJava/trivia/target/classes

# optional comma-separated list of paths to libraries. Only path to JAR file is supported.
# sonar.libraries=C:/OOR/broadleaf_commerce/src/clone-git/BroadleafCommerce/common/target/*.jar

# The value of the property must be the key of the language.
sonar.language=java

# Additional parameters
# sonar.my.property=value
